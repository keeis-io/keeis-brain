# Project: <NAME>

- **Goal (one sentence)**:
- **Status**:
- **Owner**:
- **Jira**: epic `[KEY]` (created by the `new-project` skill)
- **Docmost**: page `[URL]` in the `Projects` space
- **Code**: repo `[URL]` — code does NOT live in this repo.
- **Folders**: `specs/` (specifications) · `architecture/` (technical design) · `canvas/`
  (business models). Filled in by derivations (`derive spec|architecture|lean-canvas|bmc`).
  Technical specs migrate to the code repo once the project moves to build.
- **Next milestone**:
