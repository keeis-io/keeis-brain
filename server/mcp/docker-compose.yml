services:
  docmost-mcp:
    build: .
    container_name: keeis-docmost-mcp
    restart: unless-stopped
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      # Publicado SOLO en la IP de tailnet del ProDesk — nunca 0.0.0.0
      - "${TAILSCALE_IP}:8808:8808"
