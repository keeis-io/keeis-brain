# IP de tailnet del ProDesk (obtener con: tailscale ip -4)
TAILSCALE_IP=100.0.0.0

# API de Docmost visto DESDE el contenedor (host.docker.internal = el ProDesk)
DOCMOST_API_URL=http://host.docker.internal:3000/api

# Usuario de servicio dedicado, permisos mínimos por espacio
DOCMOST_EMAIL=claude@keeis.local
DOCMOST_PASSWORD=cambia-esto
